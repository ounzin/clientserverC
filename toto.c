#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int choice;
    printf("Please enter your choice: ");
    scanf("%d", &choice);
    printf("Your choice is: %d", choice);
}